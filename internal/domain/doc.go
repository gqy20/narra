// Package domain defines scenario configuration, mutable world state, and the
// value objects exchanged between the simulation engine and its adapters.
// It intentionally depends only on the Go standard library.
package domain
