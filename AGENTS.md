# Repository Agent Guidelines

本文件约束在本仓库中工作的自动化 Agent。目标是保护多人并行工作，保持内容与通用代码的边界，并让资源接入、视觉验收和游戏录制具备可复现的质量门禁。

## 工作区与提交

- 开始修改前必须运行 `git status --short`，识别已有修改和未跟踪文件。
- 开始任务时应记录预计修改的文件或目录范围；结束时比较预计范围、实际修改范围和暂存范围。
- 已有修改均视为他人工作；除非任务明确要求，不得覆盖、还原、移动、删除或提交。
- 普通任务默认直接在 `main` 分支修改和提交，不为每项工作额外创建功能分支或 worktree。
- 只有用户明确要求、仓库保护策略禁止直接提交，或操作具有较高隔离风险时，才创建独立 branch/worktree；使用前应说明原因。
- 多个任务共享同一工作区时，仍须明确本任务预计修改的文件范围，并将已有修改视为他人工作。
- 禁止使用 `git add .`、`git add -A` 或其他会扩大暂存范围的命令。只暂存本任务明确产生的文件或代码片段。
- 提交前必须依次检查：
  - `git diff --cached --check`
  - `git diff --cached --name-status`
  - `git diff --cached`
- 如果共享文件中混有他人修改，只能精确暂存自己的代码片段；无法安全拆分时，不得提交该文件。
- 结构定义与由其生成的 Schema、契约文件必须位于同一提交，避免源码与生成结果失配。
- `.env`、AI 设置文件、真实模型响应、诊断包和其他敏感运行配置不得提交。
- 录制成片、运行日志、临时存档、检查截图和转码中间文件不得进入普通源码提交。

## 内容与表现分层

- 剧情、人物、地点、事实、资源和表现映射应声明在对应内容包中，不得在通用 Go 或 Godot 代码中硬编码具体场景 ID。
- 人物、地点、音乐和影片统一通过 `presentation` 配置及表现资源注册层接入。新增或切换场景时，应由场景配置自动选择对应资源。
- 新资源必须保留稳定 fallback。表现资源缺失时可以降级显示，但不得阻止场景加载或游戏启动。
- 必须区分资源降级与语义降级：图片、音频和影片允许使用通用占位资源；故事语义、NPC 语言策略和玩家文案缺失时必须报错。
- 玩家可见文本不得出现 `E01`、`F02`、内部 action ID、事实 ID、策略 ID 等实现标识。
- 通用代码不得假设某一内容包的专有世界观术语；玩家文案和术语应由内容包提供。
- 修改内容结构时，应同步更新静态校验，尽量让无效引用、缺失资源和错误枚举在启动或录制前失败。

## 配置契约

- 修改配置结构时，按“字段契约、静态校验、官方内容包、运行时代码、客户端、测试、文档”的顺序推进。
- `dialogue.yml`、`presentation.yml` 中运行时必需的字段和键必须在场景加载阶段完整校验，不能等到对应界面或对话首次使用时才发现缺失。
- 新增配置键时，必须同步修改领域类型、Schema 版本门禁、校验器、所有官方内容包、API Schema、测试和相关文档。
- 通用代码只能保存配置键和结构规则，不得保存某个故事包的默认文案作为隐式兼容层。
- 配置模板中的占位符属于契约；修改占位符时必须同时检查调用参数和至少两个内容包的渲染结果。
- 内容包与存档仅接受当前 Schema；旧版本必须由作者在仓库外显式升级，不得在运行时自动补全故事语义。

## AI 行为与验证

- AI 可以明确关闭；关闭时不得发起模型请求，也不得把规则模板伪装成模型回复。
- AI 一旦启用，超时、空响应、结构错误、内容校验失败和提供方错误必须明确返回给调用方。
- 禁止用模板文本、本地规则文本、旧缓存或另一模型结果降级替代本次失败的模型调用。
- 确定性回归测试必须显式关闭 AI，不能依赖开发者机器上的 `.env`、用户设置或密钥状态。
- 真实模型测试必须使用独立入口，并覆盖成功、超时、空响应、结构错误和错误展示；网络失败不得阻断与 AI 无关的确定性门禁。
- 日志、测试输出和诊断包不得包含 API Key、Authorization 请求头或完整敏感配置。

## 批量代码修改

- 批量替换前必须统计匹配数量，并抽查无参数、模板参数、嵌套调用等代表性形态。
- 涉及函数参数、嵌套调用或模板占位符时，优先使用语法树或结构化工具，不使用宽泛正则直接改写。
- 每完成一类批量修改，立即运行格式化、对应包编译、异常形态搜索和局部 diff 检查，再继续下一类。
- 批量修改后必须搜索空参数、丢失配置键、重复参数、旧调用签名和未替换的故事专属术语。
- 临时重写工具使用完后必须删除，并确认未进入暂存区。

## 客户端初始化

- 依赖场景配置的界面不得在场景信息加载并校验完成前构建。
- 推荐启动顺序为：服务健康检查、场景配置加载、配置校验、界面构建、开放交互。
- 异步初始化阶段访问 UI 节点、音频总线或表现注册表时，必须处理对象尚未创建或配置尚未就绪的状态。
- 禁止通过增加故事专属默认值掩盖初始化顺序问题；初始化失败必须保留明确诊断信息。

## 视觉资源验收

- 人物立绘至少要在 1280×800 和 1920×1080 两种目标画面下检查。
- 人物头部、发冠和关键轮廓必须完整露出，身份字幕与操作区不得遮挡面部。
- 不得用全局统一偏移修复单个人物构图。人物差异应通过配置中的焦点、缩放或偏移解决。
- 人物入场不得同时缩放立绘和父容器；同一人物与表情的重复渲染不得重新触发入场动画，开始新动画前应终止仍有效的旧 Tween。
- 地点背景需要检查顶部导航、底部操作区和人物立绘遮挡后的有效构图。
- 开场和片尾影片播放期间必须隐藏游戏操作层；影片结束或跳过后再恢复交互界面。
- 视觉修改完成后，应生成固定场景截图或短视频复核。至少覆盖开始页、地点、地图、日志、关键行动和结局。
- 不能仅以资源原图正常作为验收依据，必须检查资源在真实 UI 容器中的最终显示效果。

## 界面信息层级

- 同一屏幕必须只有一个明确的主对象。世界标题、当前模式、当前地点和状态如果已经由顶部导航、地图标牌或主标题表达，详情栏不得换一种说法再次重复。
- 眉题、副标题、说明句和分隔线只有在提供新增信息或操作提示时才保留；“选择地点查看详情”“路线沙盘”等对当前界面不言自明的文字应删除或降为短图例。
- 信息层级不得依赖极小字号制造。小字用于状态与分类，中字号承载说明，大字承载对象名称；所有层级仍必须遵守全局最小可读字号。
- 列表中多项记录具有相同动向、缘由、状态说明时，应把共同信息汇总一次，各项卡片只保留身份、状态和差异信息，避免逐项复述相同句子。
- 地图默认态只常驻地点、拓扑和当前可通行关系。耗时、人物计划、风险解释等二级信息应在选中、悬停或固定详情区出现；同类标牌不得铺满全部路线和节点。
- 颜色只能承担稳定语义：金色优先表示当前焦点或可通行关系，红色表示风险或阻断，普通关系使用低对比中性色。不得用大面积光晕或整圈警示让次要状态持续抢占注意力。
- 遇到看似异常的连线、节点或数量时，必须先核对内容包中的权威数据，再决定删除还是调整表现。真实路线因节点近乎共线而像定位杆时，应调整曲率或避让，不得擅自删改路线语义。
- 信息层级调整后应增加回归断言，至少覆盖：冗余标题不再出现、默认态标牌数量受控、交互态只展开一个焦点详情、人物摘要不会无上限铺开。
- 视觉复核应同时查看全屏构图和关键局部，并把参考目标与实机截图裁到可比较的区域后并排检查。字体、间距、颜色、资源质量和玩家文案都要逐项确认，不能仅凭单张截图印象验收。
- 1280×800 与 1920×1080 验收必须确认运行时内容画布和最终输出状态。若项目使用固定逻辑画布加拉伸模式，应分别记录逻辑画布尺寸与 Movie Writer 输出尺寸，避免把仅缩放输出误认为响应式布局已经验证。

## 音视频规范

- Godot 运行时影片使用项目正式支持的格式；审阅和分发版本使用 H.264/AAC MP4。
- 标准录制成片为 1920×1080、30 FPS、`yuv420p`、SAR 1:1，并同时包含视频流和音频流。
- 4K 录制必须同时锁定 Movie Writer 输出和运行时内容画布为 3840×2160；不得只放大输出容器。正式 4K 链路使用质量 1.0 的 MJPEG 中间源和 H.264 CRF 14 成片。
- 音频必须进入 Music、Ambient、Event 或 UI 总线，不得绕过总线直接播放。
- 音乐和环境声切换必须包含淡入淡出，不得因切换场景或重复渲染而叠加播放。
- 等待服务启动、请求完成和演出结束时，应使用健康检查或明确的状态条件。固定延时只用于控制观众可见的镜头停留时间。
- Movie Writer 的录制路线超时必须使用 SceneTree 模拟时间；不得用墙钟时间衡量固定帧率离线渲染，否则高质量编码可能在仅推进少量游戏帧时误判超时。
- 完整录制前应先运行快速路线预检，确认场景、地点、行动、结局和媒体资源均可到达或加载。
- 录制必须使用独立临时存档，并在结束后回收本次启动的服务进程和临时目录。

## 验证顺序

按修改范围逐级验证，开发过程中优先使用快速检查，准备提交时再运行完整门禁：

1. 运行格式、解析、内容引用和资源路径检查。
2. 运行与修改范围对应的定向测试。
3. 运行目标场景的快速验证或短路线烟测。
4. 准备提交时运行完整 Go 与 Godot 门禁。
5. 正式录制前运行无画面或快速路线预检。
6. 录制后校验视频流、音频流、分辨率、时长、像素格式和像素宽高比。

常用验证入口：

- Go 全量验证：`./tools/verify.ps1`
- 内容编译：`go run ./cmd/fantu-content validate data/<world>`
- 内容图检查：`go run ./cmd/fantu-content graph data/<world>`
- 内容批量试玩：`go run ./cmd/fantu-content simulate data/<world> --runs 200 --seed 1`
- Godot 验证：`./tools/verify-godot.ps1`
- 天启快速验证：`./tools/verify-tianqi.ps1 -Mode fast`
- 天启完整验证：`./tools/verify-tianqi.ps1 -Mode full -Fresh`
- 标准游戏录制：`./tools/record-gameplay.ps1`

执行 Godot 门禁时必须明确选择 AI 模式。确定性门禁应确保真实 API Key 不会被 `.env` 或用户设置重新启用；真实模型冒烟应单独记录为非确定性验证。

正式内容包必须加入 `fantu-content` 与 Godot 可移植性门禁。新增世界的验收目标是只增加 `data/<world>` 和可选素材，不为其增加 Go/GDScript 场景 ID、人物 ID、地点 ID 或 Flag 分支。世界导演启用 AI 时，存档必须保存权威 `directive_id` 决策并在加载时重放，禁止重新请求模型或静默改用确定性选择。

## 安全操作

- 临时存档必须创建在经过绝对路径校验的临时目录中。
- 启动本地服务前应检查端口占用；不得复用来源不明的已有服务完成隔离测试或录制。
- 只终止由当前任务启动并持有进程句柄或明确 PID 的进程。
- 不得删除或清理无法确认归属的文件和目录。
- 不得使用破坏性 Git 命令清理共享工作区。
- 遇到权限、归属或修改范围不明确时，应停止相关写操作并向用户说明。
